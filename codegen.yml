overwrite: true
schema: "https://cms.webshaped.de/api"
documents: null
generates:
  src/types/generated/graphql.d.ts:
    plugins:
      - typescript
      - typescript-operations
  # ./graphql.schema.json:
  #   plugins:
  #     - "introspection"
